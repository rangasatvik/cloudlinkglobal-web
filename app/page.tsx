export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center px-6 text-center">
      <h1 className="text-5xl font-bold mb-6">
        Detect AWS cost regressions caused by deploys
      </h1>

      <p className="text-lg text-gray-npm run dev
 max-w-2xl mb-8">
        Cloudlink monitors your services and alerts you when a deployment
        increases cost — before it becomes a monthly surprise.
      </p>

      <button className="px-6 py-3 rounded-md bg-white text-black font-semibold">
        Join Early Access
      </button>
    </main>
  );
}

